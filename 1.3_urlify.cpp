#include <iostream>

using namespace std;

char *urlify(char *s);

int main()
{

    return 0;
}

char *urlify(char *s)
{
    char ctr[] = "%20";

    int n = sizeof(s) / sizeof(s[0]);
    int i = 0;
}